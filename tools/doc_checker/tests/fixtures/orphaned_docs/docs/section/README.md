# Section Index

This nested index is **not** linked from the top-level `README.md` or
`docs/README.md`, but it is itself a documentation index, so it acts as an
additional traversal entry point. It links onward to [nested](nested.md).
