# Orphaned Doc

This file exists under `docs/` but is intentionally not linked from
`README.md`, `docs/README.md`, or any other documentation index in this
fixture. The orphaned-docs check must report it.
