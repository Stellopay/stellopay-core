# Migration scripts generated artifacts (do not commit)
scripts/migrations/.env.migration
backups/

# Local environment files — may contain Stellar secret keys. Never commit.
.env
.env.*
!.env.example
